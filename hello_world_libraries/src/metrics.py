import numpy as np


def get_mean(arr) -> float:
    """
    Args:
        arr: list-like object with numerical values
    Returns:
        mean of the elements of the array
    """

    return np.mean(arr)
